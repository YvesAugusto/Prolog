adjacente(X, Y, L) :- viz(X, Y, L).
adjacente(X, Y, [Y|T]) :- last(T, X).
viz(X, Y, [X,Y|_]).
viz(X, Y, [_|T]) :- viz(X, Y, T).
vizinhos(X,Lista):- adjacente(A,X,Lista), write('Previous: '), write(A),nl, adjacente(X,S,Lista), write('Next: '), write(S), nl.

vizinhos(b, [k,b,y,b,t,u]).

%https://swish.swi-prolog.org/p/uMqkhaMo.swinb
